import 'package:flutter/material.dart';

class PhysicalDetailsScreen extends StatelessWidget {
  const PhysicalDetailsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(

    );
  }
}
